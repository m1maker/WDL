[package]
name = "swell_accesskit_shim"
version = "0.1.0"
edition = "2021"
rust-version = "1.85"

[lib]
crate-type = ["staticlib"]

[dependencies]
accesskit = { path = "../../../../../accesskit/accesskit" }
accesskit_unix = { path = "../../../../../accesskit/adapters/unix" }
zbus = { version = "5.14.0", default-features = false, features = ["async-io", "blocking-api"] }
